# MyCoder — 本地 Coding Agent Harness 依赖清单
# 约束:全部本地运行,不需要任何云端服务。
# 运行时核心仅依赖 PyYAML(其余全部使用 Python 标准库)。
PyYAML>=6.0

# ---- 本地 OpenAI 兼容后端(可选)----
# 若使用 requests 版客户端,取消下面注释;默认实现用标准库 urllib,可不装。
# requests>=2.31

# ---- 测试 / 评测 ----
pytest>=8.0